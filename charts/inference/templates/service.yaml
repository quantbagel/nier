apiVersion: v1
kind: Service
metadata:
  name: {{ include "nier-inference.fullname" . }}
  labels:
    {{- include "nier-inference.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "nier-inference.selectorLabels" . | nindent 4 }}
