apiVersion: v1
kind: Service
metadata:
  name: {{ include "nier-dashboard.fullname" . }}
  labels:
    {{- include "nier-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nier-dashboard.selectorLabels" . | nindent 4 }}
